@{
    ViewData["Title"] = "Home";
}
@section Scripts {
<script>
    // Redirect based on authentication state (JWT in localStorage)
    if (localStorage.getItem('hcrm_token')) {
        window.location.replace('/Patient');
    } else {
        window.location.replace('/Account/Login');
    }
</script>
}
